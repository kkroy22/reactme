import styled from 'styled-components';
import { GRANITE_GRAY } from '../../theme/variables';
import { Relative } from '../../theme/grid';

export const Title = styled.h1`
  margin-top: 0;
  color: ${GRANITE_GRAY};
  text-align: center;
`;
